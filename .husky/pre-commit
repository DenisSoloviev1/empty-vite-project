#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

npm run format:check || (
    echo '🎭 Prettier (громким голосом):
            "О, горе мне! Какой хаос в отступах!
            Это не код — это трагедия Шекспира!"
            Исправь. Немедленно.'
    false
)

npm run lint:check || (
    echo '🎬 ESLint (в смокинге, с моноклем):
            "Сэр, ваш код нарушает этикет.
            Это неприемлемо в высшем обществе."
            Поправьте манеры.'
    false
)

npm run types:check || (
    echo '🎩 TypeScript (с цилиндром и тростью):
            "Мой золотой, ваши типы — это скандал!
            Это дурной тон в приличном обществе."
            Приведите типы в порядок.'
    false
)

npm run build || (
    echo '🎪 Цирк уехал, а билд остался.
            Клоуны в панике. Слон в node_modules.
            Это конец шоу.'
    false
)

echo '🌟 БРАВО! Занавес! Код прошёл все круги ада!
       Commit принят. Аплодисменты.
       
       npm run format:check
       npm run lint:check
       npm run types:check
       npm run build'
